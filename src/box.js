import React from 'react';
import styled from 'styled-components';

const StyledBox = styled.div`
  height: 325px;
  background-color: #f0f0f0;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  cursor: pointer;
  text-align: center;
  padding: 10px;
  &:hover {
    transform: scale(1.05);
    transition: transform 0.2s ease-in-out;
  }
`;

// Props are passed to the Box component for flexibility
const Box1 = ({ onClick, imgSrc, line1, line2 }) => (
  <StyledBox onClick={onClick}>
    <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
    <div>{line1}</div>
    <div>{line2}</div>
  </StyledBox>
);
const Box2 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box3 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box4 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box5 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box6 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box7 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box8 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box9 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box10 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box11 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box12 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box13 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box14 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
  const Box15 = ({ onClick, imgSrc, line1, line2 }) => (
    <StyledBox onClick={onClick}>
      <img src={imgSrc} alt="Content" style={{ width: '100%', height: 'auto' }} />
      <div>{line1}</div>
      <div>{line2}</div>
    </StyledBox>
  );
                         


export default Box1;
